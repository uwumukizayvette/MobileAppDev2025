  /*An arrow function is a shorter syntax for writing functions that only contain a single expression.
It returns the result of that expression automatically.*/

    int square(int x) => x * x;
void main() {
  print(square(5));//Output:25
}
