void main() {
  int marks = 80;
  String result = marks >= 50 ? 'Pass' : 'Fail';
  print(result); // Output: Pass
}

/*
Explanation:
- Short form of `if-else`, useful for assigning based on condition.
Contrast:
- Compact but harder to read than `if-else` when logic is complex.
*/
