void main() {
  Map<String, int> marks = {'Math': 85, 'English': 78};
  marks['Science'] = 90;
  print(marks);
}
/*
Explanation:
- Maps hold key-value pairs. Keys must be unique.
- We use subject names as keys and their marks as values.
Contrast:
- Lists are indexed numerically, but maps use custom keys.
- Sets don’t have key-value structure.
*/
