//In Dart, an implicit return type means that the function does not explicitly state the return type, and Dart infers it automatically
// based on the return value or the function body. 
 
 square(x) => x * x;

 void main(){
    print(square(4));
 }