void main() {
  // For loop with counter
  for (int i = 0; i < 5; i++) {
    print('Index: $i');
  }

  // For-in loop
  List<String> colors = ['red', 'green', 'blue'];
  for (var color in colors) {
    print(color);
  }
}
